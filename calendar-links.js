(async function () { const response = await OnkofizjoApi.get('/api/appointments', 'data/demo-appointments.json'); const appointments = response.data.appointments || []; document.querySelectorAll('a[href="patient.html"]').forEach((link) => { const row = link.closest('.row'); const name = row?.querySelector('div strong')?.textContent.trim(); const appointment = appointments.find(item => item.patient === name); if (appointment) link.href = `patient.html?patientId=${encodeURIComponent(appointment.patientId)}`; }); })();
